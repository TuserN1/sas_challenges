const version = 280
let color = "blue"
const description = "a wheeled motor vehicle designed primarily to run on roads, seat one to eight people, and transport passengers."
let work = true
console.log("the car version is " + version +" its color is " + color );
